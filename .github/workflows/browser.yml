name: Browser regression

on:
  # Also runs on pull requests. Previously this was push-to-main only, so a
  # broken browser suite was only discovered after merging -- which is how the
  # 2026-08-03 service-worker reload regression and a missing e2e service stub
  # both reached main. Quality already runs on pull_request; this closes the
  # matching gap for Playwright.
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  playwright:
    name: Playwright desktop and mobile
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - name: Set up Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7
        with:
          python-version: "3.12"

      - name: Install uv
        run: python -m pip install uv

      - name: Set up Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: "22"
          cache: npm

      - name: Install Python dependencies
        run: uv sync --locked

      - name: Install JavaScript dependencies
        run: npm ci

      - name: Install Chromium
        run: npx playwright install chromium

      - name: Run browser regression suite
        run: npm run test:e2e
